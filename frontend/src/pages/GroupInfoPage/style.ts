import styled from "styled-components";

const GroupInfoPageContent = styled.div`
  height: calc(100% - 80px);
  overflow-y: auto;
  overflow-x: hidden;
  padding-bottom: 16px;
`;

export { GroupInfoPageContent };
